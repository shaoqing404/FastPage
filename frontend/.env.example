# When served via the Nginx Docker container, use relative path.
# The Nginx proxy will forward /api requests to the backend API container.
# For local dev without Docker, this would typically be http://localhost:22223/api/v1
VITE_API_BASE_URL=/api/v1
